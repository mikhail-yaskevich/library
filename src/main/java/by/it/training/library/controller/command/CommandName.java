package by.it.training.library.controller.command;

public enum CommandName {
    NO_SUCH_COMMAND, REGISTRATION, LOGIN, LOGOUT, BOOKS, AUTHORS, ABOUT, LOCALE, GENERAL, SUBSCRIPTIONS, TAKE, BRING,
    CANCEL, BOOK, RESERVE, AUTHOR, READERS
}
